public class LiquidContainer extends Container implements IHazardNotifier{

    public LiquidContainer(int mass, int height, int tareWeight, int weight, int depth, int maxPayload) {
        super(mass, height, tareWeight, weight, depth, maxPayload);
    }
}
