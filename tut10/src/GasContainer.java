public class GasContainer extends Container implements IHazardNotifier {

    public GasContainer(int mass, int height, int tareWeight, int weight, int depth, int maxPayload) {
        super(mass, height, tareWeight, weight, depth, maxPayload);
    }
}
